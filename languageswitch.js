function changeLanguage(language) {
    if (language === 'de') {
        window.location.href = 'index_de.html'; // Verweise auf die deutsche Version
    } else if (language === 'en') {
        window.location.href = 'index_en.html'; // Verweise auf die englische Version
    }
}